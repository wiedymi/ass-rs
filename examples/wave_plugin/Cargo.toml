[package]
name = "wave_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ass-core = { path = "../../crates/ass-core", features = ["dynamic-loading"] }

[features]
default = []